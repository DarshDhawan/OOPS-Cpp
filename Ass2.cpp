#include <iostream>
#include <string>

using namespace std;

int main()
{
    string pass;
    int u,l,d,s;
    cout << "\n\nPassword Strength Checker\n\nEnter Password : ";
    cin >> pass;
    for(char ch:pass)
    {
        if(ch)
    }
    return 0;
}